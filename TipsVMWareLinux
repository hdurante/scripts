echo Para podercompartir carpetas con el hosta agregar:
echo vmhgfs-fuse /mnt/hgfs fuse defaults,allow_other 0 0
echo en 

sudo nano /etc/fstab

echo Para poder compartir la pantalla en zoom
echo activar:
echo WaylandEnable=false
sudo nano /etc/gdm3/custom.conf
